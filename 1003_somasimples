#include <stdio.h>

int main() {
    int A, B, SOMA;

    // Leitura dos valores inteiros
    scanf("%d", &A);
    scanf("%d", &B);

    // Cálculo da soma
    SOMA = A + B;

    // Impressão do resultado formatado
    printf("SOMA = %d\n", SOMA);

    return 0;
}